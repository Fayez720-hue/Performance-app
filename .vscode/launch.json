{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Next.js in Debug Mode",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}